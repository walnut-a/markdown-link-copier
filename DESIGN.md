# Markdown Link Copier 设计原则

## 核心定位

Markdown Link Copier 是一个专注于“快速复制准确、干净链接”的轻量浏览器工具。

产品必须保持极简、快速和极小体积，但极简不等于简陋。用户看不到多余功能，也不应该遇到含糊状态、粗糙排版、不一致交互或需要猜测的结果。

## 功能准入

新增能力必须同时满足：

1. 直接提高标题识别、链接清理、复制速度或结果可信度。
2. 不打断点击扩展后自动复制的主路径。
3. 优先复用现有页面、权限和浏览器能力。
4. 不为了低频便利引入常驻后台、外部服务或大体积依赖。
5. 能以清晰设置、回退行为和测试说明其边界。

需要新增持久权限、Service Worker、隐藏页面或复杂跨上下文通信的功能，默认不加入；只有当核心使用收益明显高于权限和维护成本时才重新评估。

快捷键属于核心复制路径的例外：可以使用仅响应 `commands` 事件的 Manifest V3 Service Worker，但不得常驻、轮询、访问网络或扩大标签页权限；空闲时必须由浏览器自动休眠。

## 界面原则

- 弹窗只表达当前状态、复制结果和必要的辅助入口。
- 主路径自动完成，不增加确认步骤或多余按钮。
- 信息层级依次是：当前状态、复制结果、标题来源、辅助设置。
- 使用克制的色彩、间距和几何图标建立质感，不依赖装饰性效果。
- 加载、成功、失败和键盘焦点必须清晰可辨。
- 长标题、长链接和本地化文本不得造成横向溢出。
- 设置页可以承载高级能力，但默认选项必须安全且容易理解。

## 技术原则

- 保持零运行时第三方依赖，优先使用浏览器原生 API。
- 权限按最小必要范围声明，用户主动触发时优先使用 `activeTab`。
- 后台命令只在用户主动触发快捷键时按需唤醒，不维护页面状态。
- 快捷键反馈使用隔离样式的临时页面提示，自动移除，不申请通知权限。
- 发布包只包含运行文件，不包含测试、文档或商店原始素材。
- 标题与 URL 规则优先采用通用标准和可验证回退，不堆叠站点专用补丁。
- 行为变化使用测试固定；视觉变化必须检查真实浏览器渲染。

## 非目标

- 网页收藏、稍后阅读和历史管理。
- 账号系统、云端服务或远程同步后端。
- AI 摘要、改写标题或内容分析。
- 为覆盖所有低频场景而持续扩大权限和后台架构。
